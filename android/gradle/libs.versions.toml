[versions]
agp = "8.13.0"
kotlin = "2.2.20"

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }

[libraries]
# Intentionally empty. This module is a release-pipeline placeholder; the real
# dependency graph arrives with android-foundations.
