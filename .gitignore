#no need to track zip file of codes
**/code
*.zip

# generated by `npm run switch:chrome|switch:firefox`;
# manifest.chrome.json / manifest.firefox.json are the sources of truth
frontend/app/manifest.json

# build output of `npm run package`
frontend/publish/dist/

# per-machine Claude Code permissions; the shared skills/commands are tracked
.claude/settings.local.json

# android build output.
# gradle/wrapper/gradle-wrapper.jar is deliberately NOT ignored -- the wrapper is
# only reproducible if the jar is committed.
android/.gradle/
android/build/
android/*/build/
android/local.properties
android/app/release/
*.keystore
*.jks

# django related files
**/static
*.db
*.env

# context portal
context_portal/

# virtual env
**/VENV_Django_4_1_EksiEngel

# py cache
**/__pycache__

# test folder (no need for now)
/test.clinerules-default
.roo/system-prompt-architect
.roo/system-prompt-ask
.roo/system-prompt-code
.roo/system-prompt-debug
.roo/system-prompt-test
.roomodes
.clinerules-default
.roomodes.json:Zone.Identifier
.clinerules
*:Zone.Identifier
AGENTS.md